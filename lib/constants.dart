/// App-wide constants — single source of truth for version, build info, etc.
class AppConstants {
  static const appVersion = '1.1.0';
  static const buildNumber = 2;
  static const buildDate = '2026.02';
  static const versionLabel = 'Version $appVersion (Build $buildDate)';
}
